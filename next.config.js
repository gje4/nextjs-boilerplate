/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'cdn11.bigcommerce.com',
            'images.contentstack.io',
        ],
    },
}


module.exports = nextConfig
